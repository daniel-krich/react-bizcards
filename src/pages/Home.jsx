import { Container } from "react-bootstrap";
import { HomeInfo } from '../features/home';

export default function Home() {
    return (
        <Container>
            <HomeInfo />
        </Container>
    );
}