import RoutesConfig from "./Routes";
import { ToastContainer } from 'react-toastify';

function App() {
    return (
        <>
            <RoutesConfig />
            <ToastContainer />
        </>
    );
}

export default App;
